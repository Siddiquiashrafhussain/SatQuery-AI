# Groq general assistant (non-evidence fallback for region chat)
#
# LOCAL development / automated tests (no API key required):
GROQ_PROVIDER=development
GROQ_MODEL=openai/gpt-oss-20b
#
# REAL Groq API (general questions only — never used for satellite evidence):
# GROQ_PROVIDER=groq_api
# GROQ_API_KEY=gsk_your_key_here
# GROQ_MODEL=openai/gpt-oss-20b
# GROQ_TIMEOUT_S=30
