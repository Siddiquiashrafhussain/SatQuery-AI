# Experiments

Research and model training live here. **Do not import from `experiments/` into `backend/app` or `frontend/`.**

## Planned areas

- `rs_vlm/` — remote-sensing VLM evaluation and fine-tuning
- `change_detection/` — ChangeFormer, BIT-CD, CVA baselines
- `optical_sar/` — fusion experiments
